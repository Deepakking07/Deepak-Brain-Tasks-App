apiVersion: v1
kind: Service
metadata:
  name: brain-tasks-service
  namespace: brain-tasks   # <-- ADD THIS
  labels:
    app: brain-tasks
spec:
  type: LoadBalancer
  selector:
    app: brain-tasks
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http

